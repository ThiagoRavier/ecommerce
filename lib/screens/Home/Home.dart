import 'package:flutter/material.dart';
import '../../components/GoldButton.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return GoldButton(
      label: 'kkkk',
    );
  }
}
